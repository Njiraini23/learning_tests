#include <stdio.h>

int main(void)

#include <stdio.h>

int main(void)

Struct Node
{
	int data;
	Node *next;
} 
Node* A;
A = NULL; // creates an empt list

Node *tmp = (Node*)malloc(sizeof (Node)  
temp->data = 2; 
temp->next = NULL;

A = temp;
